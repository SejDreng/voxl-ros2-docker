# ==========================================================
# Python packages for ROS2 NN workflow.
# Installed in both voxl-deps (dev) and voxl-runtime stages.
# ==========================================================

setuptools
wheel
catkin_pkg
numpy

# Backend 1: native PyTorch runtime
torch

# Backend 2: TFLite runtime
tflite-runtime
