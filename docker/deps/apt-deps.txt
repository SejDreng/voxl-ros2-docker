# ==========================================================
# Optional apt dependencies for NN workflow.
# Keep this file minimal to reduce image complexity.
#
# Rules:
#   - One package per line
#   - Lines starting with # and blank lines are ignored
# ==========================================================

# OpenCV runtime libs (required indirectly by ultralytics during torch.load unpickling)
libgl1
libglib2.0-0
libsm6
libxext6
libxrender1
libgbm1
libgbm-dev

# NOTE: libmodal-pipe packages are provided on VOXL host images, not generic
# Ubuntu repos used for workstation/cross Docker builds.
# Keep these out of shared apt deps to avoid build-cross failures.
