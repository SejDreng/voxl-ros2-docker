# ==========================================================
# Optional apt dependencies for NN workflow.
# Keep this file minimal to reduce image complexity.
#
# Rules:
#   - One package per line
#   - Lines starting with # and blank lines are ignored
# ==========================================================

# OpenCV runtime libs (required indirectly by ultralytics during torch.load unpickling)
libgl1
libglib2.0-0
libsm6
libxext6
libxrender1